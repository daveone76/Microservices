##=====================##
##=====================##
server:
  port: 8091


spring:
  cloud:
    config:
      server:
        uri: https://github.com/daveone76/Microservices.git
        searchPath: config-data
        username: ${GIT_USER}
        password: ${GIT_PASSWORD}

  security:
    user:
      name: root
      password:  s3ctr3t
      